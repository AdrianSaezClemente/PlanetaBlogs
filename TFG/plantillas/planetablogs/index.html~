<!DOCTYPE html>
<html lang="es">
	<head>
		{% block head %}
		<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">
	   	<link rel="stylesheet" type="text/css" href="{{STATIC_URL}}index.css" title="style" />
	   	<link rel="stylesheet" href="http://code.jquery.com/ui/1.10.1/themes/base/jquery-ui.css" />
		<script src="http://code.jquery.com/jquery-1.9.1.js"></script>
		<script src="http://code.jquery.com/ui/1.10.1/jquery-ui.js"></script>
		<script src="{{STATIC_URL}}index.js"></script>
		<script>
	   	var tmp = '{{ json_usuarios|escapejs }}';
	   	var usuario = JSON.parse(tmp);
	   	</script>
		<title>TFG</title>
		{% endblock head %}
	</head>

	<body>
		<!-- Cabecera -->
		<div id="header">
			<div id="cabecera">
				<div id="logo">
					<!--img src="{{STATIC_URL}}imagenes/planet.jpg"/-->
					<span>Educational Planet</span>
				</div>
				<div id="busca">
					<input id="buscar" type="text" style="font-style:oblique;font-size:0.7em;" name="buscar" placeholder="  Búsqueda por título" autocomplete="off">
					<select> 
						<option value="all">Todas</option>
						<option value="title">Título</option>
					</select>
				</div>
				<div id="login">
					<span id="iden"><a href="javascript:void(0);">Login</a></span>
					<span id="registro"><a href="/planetablogs/nuevousuario/">Regístrate</a></span>
				</div>
			</div>
			
			<div id="subcabecera">
				<div id="intercabecera">
					<div id="formulario">	
						<form method="POST">{% csrf_token %}
							<input id="boton_iden" type="submit" value="Identificar" />
							<input id="intro_pass" type="text" style="font-style:oblique;font-size:0.7em;" name="password" placeholder="  Introduce tu contraseña" autocomplete="off">
							<input id="intro_nick" type="text" style="font-style:oblique;font-size:0.7em;" name="nick" placeholder="  Introduce tu nick" autocomplete="off">
						</form>
					</div>
				</div>
				<span id="sub_nick"></span>
			</div>
		</div>
		<!-- Contenedor -->
		{% block contenedor %}
		<div id="contenedor">
			<!-- Contenido -->
			<div id="contenido">
				{% if lista_usuarios %}
					<div id="entradas">
					{% for entrada in entradas %}
						<div id="entrada">
							<h2><a target="_blank" href={{ entrada.usuario.url_blog }}>{{ entrada }}</a></br><span>{{ entrada.fecha }}</span></h2>
							<h3><a target="_blank" href={{ entrada.link }}>{{ entrada.titulo }}</a></h3>
							<p>{{ entrada.descripcion }}</p>
							<button id="boton" onclick="">Editar entrada</button>
						</div>
					{% endfor %}
					</div>
				{% else %}
					<p>No hay entradas en Educational Planet</p>
				{% endif %}
				<div class="pagination">
					<span class="paginas">
						{% if entradas.has_previous %}
							{% if entradas.previous_page_number == 1 %}
								<span class="pag"><a href="/planetablogs/">Anterior</a></span>
							{% else %}
								<span class="pag"><a href="?page={{ entradas.previous_page_number }}">Anterior</a></span>
							{% endif %}
						{% endif %}

						<span class="current">
							Página {{ entradas.number }} de {{ entradas.paginator.num_pages }}
						</span>

						{% if entradas.has_next %}
							<span class="pag"><a href="?page={{ entradas.next_page_number }}">Siguiente</a></span>
						{% endif %}
					</span>
				</div> 
			</div>
			<!-- Columna -->
			<div id="columna">
				<div id="interior">
					<span id="titulo_columna">Suscriptores</span></br></br></br>
					<table>
						<tbody>
						{% for usuario in lista_usuarios %}
						<tr>
							<td><a style="display:list-item;font-size:0.7em" target="_blank" href="{{ usuario.url_blog }}">{{ usuario }}</a><a style="font-size:0.7em" target="_blank" href="{{ usuario.rss }}">  (feed)</a></td>
						</tr>
						{% empty %}
						<tr><td colspan="4">No hay personas suscritas</td></tr>
						{% endfor %}
						</tbody>
					</table>
				</div>
			</div>	
		</div>
		{% endblock contenedor %}
		<!-- Pie de página -->
		<div id="subpie">
		</div>
		<div id="pie">
			<p>Soy el pie de página</p>
		</div>
	</body>
</html>
