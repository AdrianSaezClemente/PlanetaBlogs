			<!-- Contenido -->
			<div id="contenido">
				{% if lista_usuarios %}
					<div id="entradas">
					{% for entrada in lista_entradas %}
						<div id="entrada">
							<h2><a href={{ entrada.usuario.url_blog }}>{{ entrada }}</a></br>{{ entrada.fecha }}</h2>
							<h3><a href={{ entrada.link }}>{{ entrada.titulo }}</a></h3>
							<p>{{ entrada.descripcion }}</p>
							<button id="boton" onclick="">Editar entrada</button>
						</div>
					{% endfor %}
					</div>
				{% else %}
					<p>No hay entradas en Educational Planet</p>
				{% endif %}
			</div>